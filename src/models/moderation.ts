export type ModerationModel = {
    id: string,
    redirect_url: string,
    external_id: string,
    status: string,
    notes: string,
    tags: string[],
    created_at: string,
    updated_at: string,
}
